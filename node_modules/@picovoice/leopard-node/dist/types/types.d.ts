export declare type LeopardWord = {
    /** Transcribed word. */
    word: string;
    /** Start of word in seconds. */
    startSec: number;
    /** End of word in seconds. */
    endSec: number;
    /** Transcription confidence. It is a number within [0, 1]. */
    confidence: number;
};
export declare type LeopardTranscript = {
    /** Inferred transcription. */
    transcript: string;
    /** Transcribed words and their associated metadata. */
    words: LeopardWord[];
};
export declare type LeopardInitOptions = {
    enableAutomaticPunctuation?: boolean;
};
export declare type LeopardInputOptions = {
    modelPath?: string;
    libraryPath?: string;
};
export declare type LeopardOptions = LeopardInitOptions & LeopardInputOptions;
//# sourceMappingURL=types.d.ts.map